# Chief of Staff for Codex

The distributable plugin is in [codex-chief-of-staff](codex-chief-of-staff/README.md). Repository-level tests and contract fixtures live under `tests/`.

Run the complete verification suite with:

```zsh
python3 -m unittest discover -s tests -v
```
