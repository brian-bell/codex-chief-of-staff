interface:
  display_name: "Chief of Staff Work Order"
  short_description: "Execute one bounded CS work order"
  default_prompt: "Use $work-order to execute this dispatched CS work order and return the standard report."
policy:
  allow_implicit_invocation: false
