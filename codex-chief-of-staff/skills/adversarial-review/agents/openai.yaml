interface:
  display_name: "Adversarial Review"
  short_description: "Review one exact Git head independently"
  default_prompt: "Use $adversarial-review to review this exact base and head and return a structured verdict."
policy:
  allow_implicit_invocation: false
