interface:
  display_name: "Babysit PR"
  short_description: "Drive an authorized PR to merge-ready"
  default_prompt: "Use $babysit-pr to follow this PR to merge-ready without merging it."
policy:
  allow_implicit_invocation: false
